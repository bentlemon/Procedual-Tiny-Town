using System.Collections.Generic;
using UnityEngine;

public class SimpleVisu : MonoBehaviour
{
    public enum EncodeLetters
    {
        unknown = '1', save = '[', load = ']', draw = 'F', turnR = '+', turnL = '-'
    }

}
